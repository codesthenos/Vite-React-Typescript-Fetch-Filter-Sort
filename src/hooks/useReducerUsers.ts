/*import { useReducer } from "react"
import { initialState, reducer } from "../reducer/reducerUsers.ts"

export const useReducerUsers = () => {
  const [state, dispatch] = useReducer(reducer, initialState)
}*/